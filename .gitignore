# Binaries
/bin/
*.exe
*.exe~
*.dll
*.so
*.dylib

# Go build artifacts
*.test
*.out

# Environment & secrets
.env
.env.*
*.pem
*.key

# Editor
.DS_Store
.idea/
.vscode/
*.swp

# Local multi-repo dev workspace (never commit — overlays looprig locally)
go.work
go.work.sum

# Git worktrees
.worktrees/

# Claude Code project settings
.claude/

# Migration review provenance (Codex review outputs) — keep locally, don't commit
/scripts/migration/codex_review_*
